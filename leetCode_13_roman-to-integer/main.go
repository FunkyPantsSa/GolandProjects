package main

import (
	"fmt"
)

var (
	I     = 1
	V     = 5
	X     = 10
	L     = 50
	C     = 100
	D     = 500
	M     = 1000
	roman = "III"
	arr   = []int{}
)

func main() {
	//fmt.Println(len(roman))
	for i := 0; i < len(roman); i++ {
		fmt.Println(roman[i])

	}
}
func roman_number(a string) {

	switch a {
	//case buffer.Buffer(a) == "I":
	//return

	}

}
